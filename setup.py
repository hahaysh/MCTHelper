from setuptools import setup, find_packages

setup(
    name="mcthelper",
    version="1.0.0",
    description="A support tool for Microsoft Certified Trainers (MCTs)",
    author="MCT Support Team",
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
